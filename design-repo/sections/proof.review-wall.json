{
  "id": "proof.review-wall",
  "category": "PROOF",
  "purpose": "Volume social proof via a grid of short unattributed reviews - breadth of adoption rather than one deep story.",
  "citation": "ia.json sections.proof.review-wall; implementedBy About.jsx / Careers.jsx",
  "scope": "/about and /careers, which share the same review set.",
  "content": {
    "type": "object",
    "properties": {
      "tabs": { "type": "array", "items": { "type": "string", "maxWords": 2 }, "minItems": 0, "maxItems": 2, "note": "Directors/Families tab pair on /about only; Careers.jsx renders the grid without tabs" },
      "reviews": { "type": "array", "items": { "type": "string", "maxWords": 30 }, "minItems": 1, "maxItems": 12 }
    },
    "required": ["reviews"],
    "additionalProperties": false
  },
  "constraints": { "onePerPage": true, "repeatable": false },
  "motion": { "pattern": "none", "reducedMotionFallback": "n/a" }
}
